//! Shared application services over host storage, prompts, and chain transports.
//! Protocol types, derivation, and deterministic policy live in `host_logic`.

pub mod device_key;
pub mod dotns;
pub mod features;
pub mod permissions;
