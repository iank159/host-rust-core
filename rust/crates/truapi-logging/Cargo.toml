[package]
name = "truapi-logging"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Shared tracing layer for TrUAPI host artifacts"

[dependencies]
tracing = "0.1"
tracing-subscriber = { version = "0.3", default-features = false, features = ["registry", "std"] }

[lints]
workspace = true
